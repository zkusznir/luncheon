var LoginForm = React.createClass({
  render: function() {
    return (
      <div>
        <h3>You have to be logged in to enter!</h3>
        <a href="auth/facebook">Log in with Facebook</a>
      </div>
    );
  }
});
